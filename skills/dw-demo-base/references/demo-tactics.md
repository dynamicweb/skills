# Demo storytelling tactics (generic)

Universal tactics for any Dynamicweb 10 demo, regardless of vertical or domain. Sister skills (`dw-demo-pim`, `dw-demo-swift`, future sister skills) cross-reference this file rather than restating these tactics inline. Domain-specific demo tactics live in each sister skill's own `references/demo-storytelling.md` (e.g. PIM-flavoured tactics around governance-gap framing and hero-SKU walkthroughs stay in `dw-demo-pim/references/demo-storytelling.md`).

## Generic tactics

- **Audience shapes the narrative**: ecom/data leads want day-in-the-life ops demos; CFO/CIO want migration + governance ROI. Open every demo with a one-sentence framing tuned to the audience in the room -- "we'll walk through a CSR taking an EDI order from receipt to invoice" reads differently from "we'll walk through how the platform absorbs a Navision migration without a custom-controllers tax."
- **Speak the customer's words**: demo copy reads as "built for us" only when it uses the customer's own vocabulary. At demo start, read `<demo>\customer-context\` for their terminology — what they call customers / branches / orders / roles, their product nouns, their channel names, and the terms they avoid — and extract a wording glossary to `<demo>\notes\wording.md` (customer-context is read-only; transformed output goes to `notes\` per `references/customer-context.md`). Apply the glossary everywhere copy is authored: page names, paragraph copy, seeded personas, product data, the cheat-sheet page. A demo that says "branch" where the customer says "location" reads generic; matching their vocabulary is the cheapest realism win in the build.
- **`DEMO-PLAN.md` is the plan doc, and it has a contract**: it is the one artefact permitted at the demo root alongside `CLAUDE.md` and `CUSTOMISATIONS.md` (the root allowlist in `dw-demo-base/SKILL.md` "Artifact hygiene"), so a pass that is told to write it needs to know what goes in it. Five sections and nothing else: the **storyline** in one paragraph in the customer's own vocabulary (from `notes\wording.md` above); the **locked decisions** already taken, each with the reason, so a later pass does not re-open them; the **beats** in demo order, each naming the surface it is shown on; the **build order** (which pass produces what, and what each pass depends on); and the **ceilings** — what this build will not do, including the surface ceilings the install imposes (a hosted install has no filesystem rung and no SQL rung). Write it in the first pass, beside `notes\wording.md`, and keep it current as decisions land: a decision that is not in it is not locked. Assert both artefacts together at the end of that pass — `notes\wording.md` exists and carries the company name, the product noun and the persona role names, **and** `DEMO-PLAN.md` exists at the demo root carrying a beat list and a ceilings section. An artefact that a guide page orders and no skill owns is how a demo root fills with ad-hoc notes.
- **Scrape preflight — resolve the live site's redirect chain before scraping a migration target**: a prospect's company domain often 301-redirects to a differently-branded product-line domain (common in roll-up / multi-brand prospects), so scraping the literal domain from the brief can recreate the wrong site — or no site. Before any pixel work, follow and record the **full redirect chain** for the brief's domain (e.g. `curl -sIL <domain>` / a headless `browser_navigate` that logs each hop), confirm which domain actually hosts the storefront/catalog, and write the chain + the chosen canonical domain to `<demo>\extracts\` (transformed/derived data extracted FROM the brief; `extracts\` is the write target, `customer-context\` is read-only — see `references/customer-context.md`). Record the call in the plan notes so the choice is auditable.
- **A blocked reference must be re-labelled at every downstream artefact**: when the named design reference is unreachable (e.g. a bot-challenge 403 that defeats headless, headed, and plain-fetch capture alike) and the work is calibrated against a substitute, name every artefact — capture directories, ledger rows, evidence notes, hand-offs — after the source **actually captured**, carry one substitution sentence into the ledger row and evidence note, and verify by grepping the deliverable set for the blocked source's name: every hit must sit within one sentence of the disclaimer, and no directory or file name may carry it alone.
- **A plan that removes a numeric guard must name the replacement value, or an explicit "REMOVE, and here is why" — never a blank**: "replace X" with no number reads as "delete X", and an unspecified replacement becomes no cap at all (the unbounded-component failure the `doc-height` assert in [`visual-qa.md`](visual-qa.md) exists to catch). When restoring, restore the *proven* value — a different number needs a reason, not a preference.
- **Repetition of a label is not repetition of a CTA**: information emitted as column data reads as a value ("In stock" ×10 offends nobody); an interactive control emitted per row drowns the page's one real conversion CTA. Fix the affordance, not the repetition — the Swift open-catalog pattern (branch the shared price template on list-vs-detail context) and its assert set are owned by [`dw-demo-swift/references/customer-center.md`](../../dw-demo-swift/references/customer-center.md) §"The anonymous sign-in nudge".
- **Channel-specific groups + feeds (one source, N shapes)**: one SKU visible in multiple channel trees with different published shapes demonstrates "one source, N shapes" without requiring real connectors. The pattern: define one product in the catalog, create one feed per channel, and let the channel-specific group tree shape what each downstream consumer sees. The demo moment is "the same SKU shows here as DOT-WIDGET-BLACK-72 with the wholesale-partner copy and there as RES-WIDGET-ZWART-72 with the retailer-friendly copy -- same row, two reads, no custom code."

## How to consume this file

This is a routable reference: load it when planning a demo storyline, or cross-reference it from a sister skill's `references/demo-storytelling.md`. Sister skills may also paste individual bullets into their own demo-storytelling reference and add domain-flavoured cousins as needed. The two-source pattern (PIM-flavoured tactics in `dw-demo-pim/references/demo-storytelling.md` + generic tactics here) keeps both narrow and reusable.

If you're adding a new sister skill, grab whichever bullets apply, cross-reference or paste them, and add domain-flavoured cousins as needed. No need to mutate this file when adding sister skills -- it's deliberately closed.
